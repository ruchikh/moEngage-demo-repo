require 'json'

package = JSON.parse(File.read(File.join(__dir__, 'package.json')))

Pod::Spec.new do |s|
  s.name = 'CapacitorMoengageCore'
  s.version = package['version']
  s.summary = package['description']
  s.license = package['license']
  s.homepage = package['repository']['url']
  s.author = package['author']
  s.source = { :git => package['repository']['url'], :tag => s.version.to_s }
  s.source_files = 'ios/Plugin/**/*.{swift,h,m,c,cc,mm,cpp}'
  s.ios.deployment_target  = '13.0'
  s.dependency 'Capacitor'
  s.dependency 'MoEngagePluginBase','~> 4.8.0'
  s.swift_version = '5.1'
  s.prepare_command = <<-CMD
    echo // Generated file, do not edit > iOS/Plugin/MoECapacitorPluginInfo.swift
    echo "let capacitorPluginVersion = \\"#{package["version"]}\\"" >> iOS/Plugin/MoECapacitorPluginInfo.swift
  CMD
end
